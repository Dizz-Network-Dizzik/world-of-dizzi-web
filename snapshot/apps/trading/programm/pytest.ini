[pytest]
# Test-only Starlette-Deprecation gefiltert (netzwerk-einheitlich, docs/25 H-3); httpx 0.28.1
# läuft, httpx2-Migration bewusst nicht gemacht (separates Paket, nur kosmetisch).
filterwarnings =
    ignore:Using .httpx. with .starlette.testclient.
