# Dizz Management [MG] — Laufzeit-Abhängigkeiten (direkt).
# Geteilte Vertrags-Bibliothek = das VENDIERTE appkit/ (kein PyPI-Paket).
# Lokal-first/0 €: KI läuft über lokales Ollama (kein Client-SDK nötig).
# SENSIBEL: keine Cloud-/Boost-Abhängigkeit — die KI bleibt lokal (sensitivity='hoch').
# Die ChannelSource-Adapter (channels.py) sind abhängigkeitsfreie Gerüste; ein
# Plattform-/OAuth-Client (z. B. httpx-basierte API-Calls) wird erst bei Aktivierung
# des jeweiligen Kanal-Slots ergänzt. Posten bleibt v1 DORMANT (Recht/Opt-in).

fastapi              # App-Vertrag (create_app, Router, Dependencies)
uvicorn[standard]    # ASGI-Server (Entwicklung/Betrieb)
pydantic             # Schemata (Manifest, Settings, Request-Modelle)
httpx                # Ollama-/Dizzi-ID-HTTP (Test-injizierbar)
joserfc              # Dizzi-ID-Token-Prüfung (EdDSA/JWKS, RP)
cryptography         # Token-Tresor (Fernet, OAuth-Tokens fremder Plattformen)
fastmcp              # MCP-Server (read-only, stdio)
