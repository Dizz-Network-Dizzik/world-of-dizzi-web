[pytest]
# Bewusst unterdrückt (docs/25 H-3): Starlettes TestClient-Deprecation „Using httpx …
# install httpx2 instead". Test-only, 0 Laufzeit-Impact (Laufzeit-httpx 0.28.1 läuft
# einwandfrei). httpx2 = separates Paket (netzwerkweiter import-Umbau) ⇒ bewusst NICHT
# gemacht; erst angehen, wenn ein Starlette-Upgrade es erzwingt. Nutzer-Entscheid 20.06.
filterwarnings =
    ignore:Using .httpx. with .starlette.testclient.
